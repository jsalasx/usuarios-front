import { Routes } from '@angular/router';
import { UsersTableComponent } from './users-table/users-table.component';

export const routes: Routes = [
     { path: 'user-table', component: UsersTableComponent, title: 'User Table' },
];
